# FinPulse

Hackathon MVP fintech dashboard for small businesses.

## Run locally

1. Install dependencies with `npm install`
2. Make sure MongoDB is running on `mongodb://127.0.0.1:27017/finpulse`
3. Start the app with `node app.js`
4. Open `http://localhost:4000`
